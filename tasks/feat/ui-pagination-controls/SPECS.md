# SPECS — Phase 6 UI pagination controls

Branch: `feat/ui-pagination-controls`

---

### Unit — UI: prev/next, search + pagination

- [x] Paginated fetch `{ items, total }` with limit 10
- [x] Previous/next controls; search resets to page 1
- [x] Scope: `public/**` only

**Verification:** Manual pagination + search; `verify.sh` green.
