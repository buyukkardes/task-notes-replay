project:
  name: "task-notes-replay"
  description: "Task Notes API — Milestone 2 replay"

stack:
  profile: node-ts

verify:
  install: "npm ci"
  test: "npm test"
  build: "npm run build"

github:
  default_branch: main
